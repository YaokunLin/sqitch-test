[core]
	engine = pg
[target "flipr_test"]
    uri = db:pg://postgres:password@localhost:5432/flipr_test
	# plan_file = sqitch.plan
	# top_dir = .
# [engine "pg"]
	# target = db:pg:
	# registry = sqitch
	# client = psql
